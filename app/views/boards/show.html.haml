= render "shared/navbar"
.container
  .col
    .row
      .card.w-100.mt-3
        .card-header
          .d-flex.justify-content-start
            .fisrt-justify-content
              = @board.title
            .secpond-justify-content.ml-3
              - @board.tag_list.each do |tag|
                = link_to tag, "#", class: "badge badge-warning"
        .card-body
          .d-flex.justify-content-between
            .fisrt-justify-content
              %h5.card-title
                = @board.content
              %p.card-text
                = "投稿者：issei  投稿日：#{l @board.created_at}"
            .secpond-justify-content
              = link_to "戻る", root_path, class: "btn btn-primary text-right"
  .col
    .row
      .card.w-100.mt-3
        .card-body
          %h5.card-title Card title
          %p.card-text With supporting text below as a natural lead-in to additional content.

